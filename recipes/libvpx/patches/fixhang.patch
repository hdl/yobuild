--- libvpx-v1.3.0/configure.old	2014-01-17 12:24:53.000000000 -0800
+++ libvpx-v1.3.0/configure	2014-01-17 12:25:17.000000000 -0800
@@ -176,7 +176,7 @@
 # install everything except the sources, by default. sources will have
 # to be enabled when doing dist builds, since that's no longer a common
 # case.
-enabled doxygen && php -v >/dev/null 2>&1 && enable_feature install_docs
+enabled doxygen && php -v >/dev/null 2>&1 < /dev/null && enable_feature install_docs
 enable_feature install_bins
 enable_feature install_libs
 
